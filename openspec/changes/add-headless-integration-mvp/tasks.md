> 2026-09-17 用户重申：基于官方稳定版插件实现，不修复其既有问题。远端操作沿用官方语义；原子删除/重命名保护和额外服务身份机制不再作为必需交付条件，不制作上游补丁。保留特征测试及限制记录，不将上游问题标为已修复。

> 2026-09-17 执行边界：本轮在本机完成客户端实现、测试和交接；部署由其他 agent 负责，不要求用户另建服务或配置反向代理。用户随后授权使用现有知识库，并愿通过本机文件提供 token；实际接入先验证连接与只读接收。外部部署和 Hermes/实际插件互操作回执保持独立、未完成，不以本地测试冒充通过。

## 1. 基线与上游复用边界

- [x] 1.1 核实正式稳定 release `2.4.0` 与精确提交，在其基础上按 `.node-version` 和 `package.json` 固定环境，使用冻结的 pnpm 锁文件复现 auth、mirror、vault-name、build、lint；更新基线记录并确认锁文件未被意外改写。
- [x] 1.2 修正 auth 测试对旧模块路径的依赖，验证测试覆盖当前认证错误处理且通过。
- [x] 1.3 调查 mirror 恢复契约和替身行为，修复真实原因并通过原场景及针对根因的回归测试，不仅修改期望值。
- [x] 1.4 在稳定版 auth/mirror 修复后运行完整回归，验证继承测试、插件 build 与 lint 全部通过；记录旧开发提交的翻译键/lint 失败不适用于稳定版，不移植相关开发改动。
- [x] 1.5 以认证、笔记和附件链路建立复用清单，逐项记录源模块、宿主依赖、原样使用/抽取/修复/策略差异及测试；通过源码和调用关系复核无平行协议实现。
- [ ] 1.6 固定隔离测试服务端版本，建立操作、Ack、分页、删除历史及条件写能力矩阵；用有界合成用例分别记录运行证据和未知项，缺少服务端环境时明确阻塞项。
- [x] 1.7 清点继承 release 和 mirror 工作流与候选构建路径，交付不会调用旧发布流程的本地验证方案，并核对触发条件。
- [x] 1.8 验证现有接口可取得的认证主体及可选服务/Vault 标识，绑定配置端点和 Vault 名称；验证同主体凭据轮换及已知错配，披露不可取得身份的范围，不要求上游新接口或响应头。
- [x] 1.9 以 20 MiB 附件偏移 6 MiB 单字节修改及保留 mtime/大小变体探测固定服务端的同哈希异内容上传、下载和完整版本确认能力；交付实际行为证据及安全阻塞策略，不修改线上哈希字段。结果：稳定版 WS 上传不传输该修改，REST 对照能传字节但未证明原子条件写；见 `docs/headless/SERVER-CAPABILITIES.md`，不代表完整验收通过。

## 2. 最小宿主与持久化基础

- [x] 2.1 提取哈希所需时钟和范围读取依赖，保留共享算法；通过中文、emoji、空内容、换行和 10 MiB 边界两端一致性测试，并验证 20 MiB 采样盲区向量协议哈希相同但完整摘要不同。
- [x] 2.2 抽取认证与传输所需宿主 I/O，保留编解码和消息行为；验证独立 Node 认证、失败退出、取消和插件回归。
- [x] 2.3 实现受限文件系统适配和单 Vault/状态目录所有权，验证路径逃逸、符号链接替换、大小写碰撞及不同状态目录双进程争锁均被保护。
- [x] 2.4 选择并验证事务存储实现，建立带版本的操作、基线、批次和会话记录；通过持久化失败、格式不支持、记录损坏和重启读取测试。
- [x] 2.5 实现外置版本快照及原子落盘恢复记录，验证临时写入、替换和状态提交边界崩溃后能识别原版本、目标版本及外部修改。
- [x] 2.6 建立最小三方冲突记录及快照持久化，验证首次接入同路径不同内容保留双方，缺少基线被明确表示。
- [x] 2.7 实现通用受控本地创建、修改、删除和重命名入口，接收幂等标识和预期完整版本/不存在条件，由所有者执行；验证陈旧版本拒绝、重复请求及来源/目标条件，不由调用方自行加锁写文件。
- [x] 2.8 持久化并校验同步身份绑定，在启动和重连恢复前验证；分别替换端点、认证主体、Vault 名称/已取得标识、本地目录及同路径目录，确认旧 pending/删除意图不发送、文件不变、旧状态保留，同主体凭据轮换的对照用例可恢复。
- [x] 2.9 让受控本地修改、远端落盘和冲突应用共用 Vault 级互斥基础，从版本读取复核保持到文件操作及状态提交；以独立进程和复核后/替换前的确定性屏障验证两种执行顺序，并验证竞争重命名目标受保护。
- [x] 2.10 实现 `controlled`/`exclusive` 写入方式的启动门禁与受控请求恢复，在缺少声明时验证零文件副作用，在文件应用后进程崩溃的幂等重试中验证不会重复删除/重命名；明确配置声明不能替代 Ops 权限验收。

## 3. 可运行只读链路

- [x] 3.1 接入上游笔记列表、分页和下载流程，提供最小只读入口；在固定服务端验证空目录拉取、分页和已有内容冲突。
- [x] 3.2 接入上游附件下载、范围读取及分片落盘，验证跨采样阈值附件的完整摘要、乱序/缺失分片和下载中断恢复。
- [x] 3.3 对只读模式施加业务写操作门禁，检查包含服务端请求上传事件的发送记录，证明无上传、远端删除和重命名。
- [x] 3.4 接入保守批次检查点，验证 End 提前到达、局部写入失败及附件未落盘时不报告成功，重启不遗漏未完成项目。
- [ ] 3.6 完成只读里程碑集成验证，保存固定服务端、完整内容清单和插件回归结果，并更新复用清单中的实际抽取点。

## 4. 可靠笔记双向同步

- [x] 4.1 实现基于完整内容的启动扫描和周期核对，验证丢失监听事件及 mtime/大小/协议哈希均未变仍能发现编辑、扫描失败保留旧清单、扫描中修改不会形成混合版本、同步回声不吞掉后续编辑。
- [x] 4.2 在上游笔记发送路径接入持久化意图和不可变内容快照，验证保存失败不会发送，同路径新编辑进入后继操作。
- [x] 4.3 将确认提交绑定到具体发送版本，验证 Ack 丢失、重复、乱序、旧连接事件及 A 确认到达时本地已变 B 的情况。
- [x] 4.4 复用官方已有的远端前置条件与确认语义，接入未知确认的内容核对；验证缺少原子 CAS 不人为阻止官方操作，实际冲突或无法确认时保留意图，记录继承的并发窗口。
- [x] 4.5 打通笔记双向修改和重启恢复，验证两端完整内容收敛、稳定后的幂等空同步及插件回归；冲突保持未解决而不覆盖。

## 5. 附件、删除与重命名

- [x] 5.1 在上游附件上传流程接入持久化发送版本和会话，验证分片重试、会话过期、取消及 Ack 丢失后的恢复与内容核对。结果：发送内容只取自不可变快照，`sent` 先提交连接代次与 context 再发送；中断、取消或会话不再可用时不信任旧分片偏移，重新请求会话并从快照完整重传（`tests/upload.test.mjs` 的 cancel 与 expired-session 用例断言两次不同的服务端 session 与全量分片）。Ack 丢失后先完整读回，目标已存在则恢复基线而不重复发送。实际 3.5.1/3.6.1 的 9 MiB 分片附件双向、重启与幂等空同步见 `--rename-sync` 探针。
- [x] 5.2 实现经过基线核对的本地删除意图及远端删除保护，验证离线删除、历史过期、挂载/扫描失败和删除对修改冲突。结果：本地缺失只有在完整成功扫描且已确认基线等于远端版本时才生成持久删除操作；远端缺失缺少回收历史证据时只报告 `historyUnverified` 并保留本地文件；扫描失败、取消、混合读取和事务提交失败保留上次成功清单且不产生删除意图；本地编辑对远端删除进入冲突并保留 base/local/remote 快照。历史保留期到期无法在探针内等待，按“缺证据不删除”处理并保留未知项；见 `test:sync`、`test:scan`、`test:conflicts` 与 `docs/headless/VALIDATION.md`。
- [x] 5.3 接入文件重命名及目录操作的来源/目标复核，验证大小写重命名、跨目录移动、目标碰撞和重命名对修改冲突；不可靠离线识别不得先删除来源。结果：发送前复核已确认基线、远端来源版本与目标占用；远端大小写重命名不再因旧名称的同折叠变体持续失败（只读查询对变体返回不存在，写入仍拒绝）；离线重命名按新建后删除处理并断言上传顺序；目标碰撞与重命名对修改保留冲突及双方快照，不先删除目标或来源。实际 3.5.1/3.6.1 的 `--rename-sync` 含跨目录与大小写重命名。目录操作限于重命名所需父目录创建与安全校验，官方 `FolderSync*` 空目录通道仍未实现，保留在 5.6 与已知限制；见 `docs/headless/VALIDATION.md`。
- [x] 5.4 固化并发、附件、缓冲、重试和状态空间上限，验证大附件峰值资源、磁盘满和快照配额不足时保留内容并报告阻塞。
- [ ] 5.6 在固定服务端完成笔记、附件、目录操作矩阵，记录普通操作是否完成以及官方基线的已知问题；适配缺陷不得冒充上游限制，上游既有问题不要求本项目修复，不把未具备的更强保证标为通过。

## 6. 通用冲突解决接口

- [x] 6.1 定义并校验版本化冲突与决策 schema，覆盖合并、保留本地、保留远端和确认删除；通过缺少基线、非法路径和非法快照引用测试。
- [x] 6.2 实现本地冲突查询、受控详情与幂等提交入口，由进程所有者串行应用；验证重复提交不重复执行、相同标识不同载荷拒绝、入口权限有效。
- [x] 6.3 在决策执行前复核本地和远端完整版本，并复用受控本地修改的互斥边界；用复核后/应用前屏障验证与 Bot 请求串行，陈旧决策无副作用且产生新冲突，弱哈希相同或提交后未确认不能提前 resolved。
- [x] 6.4 使用确定性 resolver 完成文本、删除对修改和二进制冲突流程，验证无解决者、进程重启及决策重试时快照与等待状态保留。
- [x] 6.5 交付 Hermes Ops 可消费的冲突及受控本地修改中文契约和合成调用示例，说明预期版本、幂等、本地提交与远端成功的区别、权限隔离和身份错配行为；验证示例能在隔离客户端运行且不依赖 Hermes 专用代码。

## 7. 服务入口与可观察结果

- [ ] 7.1 补齐一次性双向和常驻入口，固定配置校验、机器可读状态及退出码；验证已知身份错配/认证主体无法验证、缺少写入约定、内容核验阻塞、超限、冲突和失败均不误报成功，并明确已确认范围及已知上游限制，不把未执行的专项审计显示为完成。
- [ ] 7.2 实现有界重连、取消和优雅退出，验证停止期间的 pending 可恢复、所有权正确释放及重复启动不会产生第二个写入者。
- [x] 7.3 检查日志、异常和状态输出的隐私边界，使用合成凭据、正文和路径断言普通输出不泄露这些值。
- [x] 7.4 增加中文运行、限制、恢复及状态迁移说明，验证隔离目录从零运行和未知状态版本拒绝启动的流程，不触发继承发布工作流。
- [x] 7.5 按用户补充要求交付 Dockerfile、Compose 与中文使用说明，允许配置上游、凭据文件、笔记目录和独立状态目录；以实际非 root 容器验证现有只读链路、完整内容和重复启动保护。明确此增量不等于双向/常驻入口完成，不发布或部署生产。

## 8. 隔离集成与上游维护验收

- [ ] 8.1 汇总可重复的协议故障注入套件，验证发送、确认、落盘、提交边界及重复/乱序事件；每个场景都以持久化状态和完整内容判定结果。
- [ ] 8.2 使用实际 Obsidian 插件与 Node 客户端在固定服务端完成双向操作、并发冲突和幂等空同步，保存脱敏版本清单与验收回执；仅两个 headless 实例的测试不能替代此项。
- [x] 8.3 汇总模块复用和补丁记录并建立后续上游合并检查清单，运行插件与 Node 的构建、测试及 lint 门禁，验证来源、两端消费者和回归覆盖可追溯。结果：`REUSE.md` 补齐共享模块的插件消费者、Node 消费者与回归入口，修复断开的补丁表并补记只读大小写变体处理；`UPSTREAM.md` 新增上游合并检查清单（稳定 tag、合并分支、差异核对、本地门禁、固定服务端探针、文档与 backport、不触发继承发布流程）。新增 `test:all` 单一门禁运行 14 个套件，插件 `build`、`build:headless`、`lint`、`git diff --check` 与 OpenSpec strict 校验均通过，锁文件未改写；见 `docs/headless/VALIDATION.md`。
- [x] 8.4 为外部 Hermes Ops 交付隔离验收脚本/步骤、契约和版本清单，明确本仓库负责受控入口而 Ops 负责 Bot 接入及限制直接写权限；通过本仓库合成调用验证交付材料，本项不编辑或部署 Ops 仓库。
- [ ] 8.5 接收并核对由外部 Ops 执行的 Bot/Obsidian 往返修改、馆长有效及陈旧决策、重启恢复业务回执，并确认 Bot 绕过入口的直接写入被拒绝；缺少环境或回执时本项保持未完成，不使用测试 resolver 结果代替。
- [ ] 8.7 汇总最终验收报告，区分客户端验证与完整 Hermes 隔离验收，列明支持范围、资源上限和阻塞限制；只有声明支持范围内所有必需验收通过才声明首版可用，并披露采样盲区、远端原子条件不足及有限身份验证等继承限制，生产部署仍单独安排。

## 延后：等待上游修复（不计入当前完成率）

用户于 2026-09-16 明确决定暂不处理大附件采样盲区。以下任务从当前首版必需范围移出，保留原编号以便追溯；未实现、未标记通过，不阻塞其余 apply。上游正式稳定版有相关修复后再评估恢复。

- 原任务 3.5 复用既有读取流程实现有界完整内容核对和周期审计，验证服务端已存储内容变化但协议哈希/增量事件未变时可发现差异；验证读回版本不稳定、审计未完成和证据不可取得时不伪报全量核验成功。
- 原任务 5.5 将同哈希异内容修改接入上传和确认生命周期，验证仅有弱哈希匹配的 Ack 不能清除新版本 pending，完整读回核对成功才提交；服务端不能传输或确认时保留快照并阻塞，不用删除重建规避。
- 原任务 8.6 执行采样盲区三端验收，分别从 Node 和实际 Obsidian 修改 20 MiB 附件偏移 6 MiB 的字节并覆盖 mtime/大小不变变体，比较实际两端文件和服务端内容；插件未上传或协议不能收敛时记录端到端失败，安全阻塞通过不能勾销功能失败。
